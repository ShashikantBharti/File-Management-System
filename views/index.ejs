<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="main w-full min-h-screen bg-zinc-900">
    <div class="form p-10 text-white">
      <form action="/create" method="post">
        <input class="block px-5 w-full rounded-md outline-none py-2 bg-zinc-800" type="text" placeholder="Title"
          name="title">
        <textarea class="block px-5 w-full rounded-md outline-none mt-2 py-2 bg-zinc-800 resize-none"
          placeholder="Description" cols="22" rows="4" name="description"></textarea>
        <button type="submit" class="btn mt-2 bg-blue-600 px-5 py-2 rounded-md">Create Tasks</button>
      </form>
    </div>
    <div class="tasks flex flex-wrap gap-3 p-10">
      <% if(files.length> 0) { %>
        <% files.forEach(function(file) { %>
          <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
            <h1 class="text-white text-2xl">
              <%= file.replace(/.txt$/, "" ).replace(/_/g, ' ' ) %>
            </h1>
            <div class="flex justify-between items-center">
              <a class="text-blue-500 inline-block mt-3" href="/file/<%= file %>">Read More.</a>
              <div>
                <a class="text-zinc-500 inline-block mt-3 ms-4" href="/edit/<%= file %>">Edit</a>
                <a class="text-red-500 inline-block mt-3 ms-4" href="/delete/<%= file %>">Delete</a>
              </div>
            </div>
          </div>

          <% }) %>
            <% } else { %>
              <h3 class="text-zinc-600">No Tasks Yet.</h3>
              <% } %>
    </div>
  </div>
</body>

</html>